export function portfolio() {
    const HTML = `
        <section class="container">
            <div class="row">
                <div class="col-12">
                    
                </div>
            </div>
        </section>`;
    document.body.insertAdjacentHTML('beforeend', HTML);
}
